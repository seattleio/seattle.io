<%= form_for(@topic) do |f| %>
  <div class="admin-toolbar">
    <h1 class="admin-title">Create new topic</h1>

    <ul class="admin-toolbar-buttons">
      <li><%= f.submit :class => 'button' %></li>
    </ul>
  </div>

  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, class: 'title-input', placeholder: 'Topic name' %>
  </div>
  
  <div class="field">
    <%= f.text_area :description, class: "content-input", placeholder: 'Describe the topic (using markdown)' %>
  </div>
<% end %>