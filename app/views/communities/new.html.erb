<%= form_for(@community) do |f| %>
  <div class="admin-toolbar">
    <h1 class="admin-title">Create new community</h1>

    <ul class="admin-toolbar-buttons">
      <li><%= f.submit :class => 'button' %></li>
    </ul>
  </div>

  <% if @community.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@community.errors.count, "error") %> prohibited this community from being saved:</h2>

      <ul>
      <% @community.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, class: 'title-input', placeholder: 'community name' %>
  </div>
  
  <div class="field">
    <%= f.text_area :description, class: "content-input", placeholder: 'Describe the community (using markdown)' %>
  </div>
<% end %>