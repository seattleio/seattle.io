<% if @topics.size > 0 %>
<section id="topics" class="group small">
  <h1 class="group-title">Topics:</h1>
  <% @topics.each do |t| %>
    <div class="topic group-item">
      <h2><%= link_to t.name, t %></h2>
    </div>
  <% end %>
</section>
<% end %>